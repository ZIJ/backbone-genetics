define(['underscore', 'backbone'],
    function(_, Backbone){
        'use strict';
        var GeneticStringModel = Backbone.Model.extend({
            initialize: function(){

            }
        });
        return GeneticStringModel;
    });