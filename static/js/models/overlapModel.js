define(['underscore', 'backbone'],
    function(_, Backbone){
        'use strict';
        var OverlapModel = Backbone.Model.extend({
            initialize: function(){

            }
        });
        return OverlapModel;
    });